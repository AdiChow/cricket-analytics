# Cricket Intelligence Frontend

React, Vite, TypeScript, and Tailwind CSS frontend for the Cricket Intelligence Platform.

## Run locally

```bash
npm install
npm run dev
```

The API base URL is configured through `VITE_API_BASE_URL` in `.env`.

## Verify

```bash
npm run lint
npm run build
```
